{% extends 'base.html.twig' %}

{% block title %}Books{% endblock %}
 {% block body %}
 <h1>Livres {{category.name}}</h1>

<article>
 {% for book in books %}
    <div class="container"> 
        {% if book.imageUrl %}
        <img src= "{{book.imageUrl}}" alt="{{book.title}}"/>
        {% endif %}      
        <p> <strong>{{book.title}} </strong>: €{{book.price}}</p>
        {# {% for book in book.categories %}
        <p>{{book.categories}}</p>
        {% endfor %} #}
        

        <nav>
            <a href="{{path('app_admin_book_update', {id:book.id})}}">Modifier</a>
            <a href="{{path('app_admin_book_delete', {id:book.id})}}">Suprimer</a>
        </nav>
    </div>
 {% endfor %}
 </article>
 

 {% endblock %}